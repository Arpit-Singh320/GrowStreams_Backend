'use client';

// Stubbed V3 component for Launch branch deployment
// Full implementation is in V3 branch
export default function VaultDashboard() {
  return (
    <div className="p-8 text-center text-provn-muted">
      <p>Multi-token vault dashboard (V3 feature)</p>
      <p className="text-sm mt-2">Use /app/vault for VARA/GROW vault</p>
    </div>
  );
}
